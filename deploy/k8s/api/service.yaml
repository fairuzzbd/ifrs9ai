---
apiVersion: v1
kind: Service
metadata:
  name: blips-api
  namespace: blips
  labels:
    app.kubernetes.io/name: blips-api
    app.kubernetes.io/component: api
spec:
  selector:
    app.kubernetes.io/name: blips-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
