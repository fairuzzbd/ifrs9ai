---
apiVersion: v1
kind: Service
metadata:
  name: blips-frontend
  namespace: blips
  labels:
    app.kubernetes.io/name: blips-frontend
    app.kubernetes.io/component: frontend
spec:
  selector:
    app.kubernetes.io/name: blips-frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
